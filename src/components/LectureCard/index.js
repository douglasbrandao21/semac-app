import React from 'react';
import { CardContainer, LectureName, SpeakerName } from './styles';

const LectureCard = () => (
  <CardContainer>
    <LectureName />
    <SpeakerName />
  </CardContainer>
);

export default LectureCard;
